# Claude Code Context

This repository uses `AGENTS.md` as the canonical assistant instruction file.

Read `AGENTS.md` before doing project work. Keep this file as a thin compatibility wrapper so Claude-specific context does not drift from the shared agent guidance.
